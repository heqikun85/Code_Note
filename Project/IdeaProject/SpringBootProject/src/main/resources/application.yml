spring:
  profiles:
    active: test
---
spring:
  config:
    activate:
      on-profile: dev

server:
  port: 80
---
spring:
  config:
    activate:
      on-profile: prod

server:
  port: 81
---
spring:
  config:
    activate:
      on-profile: test

server:
  port: 82

